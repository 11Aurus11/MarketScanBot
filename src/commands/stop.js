export default (bot) => {
  bot.command('stop', (ctx) => {
    ctx.reply('Рад был помочь 🙂 Бот остановлен. Запустите код снова.');
    bot.stop();
  });
};
